---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: virtualkeys.auth.litellm.ai
spec:
  group: auth.litellm.ai
  names:
    kind: VirtualKey
    listKind: VirtualKeyList
    plural: virtualkeys
    singular: virtualkey
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VirtualKey is the Schema for the virtualkeys API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VirtualKeySpec defines the desired state of VirtualKey
            properties:
              budgetDuration:
                description: BudgetDuration is the duration of the budget
                type: string
              keyAlias:
                description: KeyAlias is the user defined key alias
                type: string
              maxBudget:
                description: MaxBudget is the maximum budget for the key
                type: string
              models:
                description: Model names that a user is allowed to call. If empty,
                  all models are allowed.
                items:
                  type: string
                type: array
              teamID:
                description: TeamID is the team ID for the key
                type: string
              userID:
                description: UserID is the user ID of the key
                type: string
            type: object
          status:
            description: VirtualKeyStatus defines the observed state of VirtualKey
            properties:
              budgetDuration:
                description: BudgetDuration is the duration of the budget
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: CreatedAt is the date and time when the key was created
                type: string
              expires:
                description: Expires is the date and time when the key will expire
                type: string
              keyAlias:
                description: KeyAlias is the user defined key alias
                type: string
              keyID:
                description: KeyID is the generated ID of the key
                type: string
              keyName:
                description: KeyName is the redacted secret key
                type: string
              maxBudget:
                description: MaxBudget is the maximum budget for the key
                type: string
              models:
                description: Model names that a user is allowed to call. If empty,
                  all models are allowed.
                items:
                  type: string
                type: array
              secretRef:
                description: SecretRef is the reference to the secret containing the
                  key
                type: string
              updatedAt:
                description: UpdatedAt is the date and time when the key was last
                  updated
                type: string
              userID:
                description: UserID is the unique user id - used for tracking spend
                  across multiple keys for same user id.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
