apiVersion: v1
kind: Service
metadata:
  name: litellm-example-loadbalancer
  namespace: litellm
  labels:
    app: litellm-litellm-example
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 4000
      protocol: TCP
      name: http
  selector:
    app: litellm-litellm-example
